import React from 'react';

const Pagination = () => {
    return null; /* (
        <div className={classes.wrapper}>
            <div className={classes.}
        </div>
    ) */
};

export default Pagination;
