import React from 'react';
import classes from './styles.module.css';

const Divider = ({ style }) => {
    return <div style={style} className={classes.divider}></div>;
};

export default Divider;
